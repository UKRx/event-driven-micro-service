{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://shoplite.com/events/OrderPlaced.v1.schema.json",
  "title": "OrderPlaced",
  "description": "Event published when a customer places an order",
  "type": "object",
  "required": ["type", "version", "orderId", "customerId", "items", "traceId", "correlationId", "ts"],
  "properties": {
    "type": {
      "type": "string",
      "const": "OrderPlaced",
      "description": "Event type identifier"
    },
    "version": {
      "type": "integer",
      "const": 1,
      "description": "Event schema version"
    },
    "orderId": {
      "type": "string",
      "format": "uuid",
      "description": "Unique order identifier"
    },
    "customerId": {
      "type": "string",
      "description": "Customer identifier (from JWT sub claim)"
    },
    "items": {
      "type": "array",
      "minItems": 1,
      "description": "List of items in the order",
      "items": {
        "type": "object",
        "required": ["sku", "qty"],
        "properties": {
          "sku": {
            "type": "string",
            "minLength": 1,
            "description": "Product SKU"
          },
          "qty": {
            "type": "integer",
            "minimum": 1,
            "description": "Quantity ordered"
          }
        }
      }
    },
    "traceId": {
      "type": "string",
      "format": "uuid",
      "description": "Distributed tracing identifier"
    },
    "correlationId": {
      "type": "string",
      "format": "uuid",
      "description": "Business correlation identifier"
    },
    "ts": {
      "type": "string",
      "format": "date-time",
      "description": "Event timestamp (ISO-8601)"
    }
  },
  "additionalProperties": false
}
